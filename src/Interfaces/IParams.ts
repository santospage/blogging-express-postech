export default interface QueryParams {
  pageSize?: string;
  page?: string;
  ordering?: string;
}

export default interface SearchParams {
  title?: string;
  detail?: string;
  resume?: string;
  category?: string;
}
